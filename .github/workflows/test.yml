name: test

on:
  push:
    branches: ['**']
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - run: npm ci

      # The golden fixtures are the quality gate for the whole approach: a
      # JavaScript reimplementation of METEOR's kernel is only trustworthy for
      # as long as it keeps reproducing METEOR's own output.
      - run: npm test

      - run: npm run build
